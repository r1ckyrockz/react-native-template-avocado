import React from 'react';

import { Screen, Text } from '../../theme';

export function Register(): JSX.Element {
  return (
    <Screen>
      <Text variant="headline" marginTop="m" textAlign="center">
        Let's get started
      </Text>
    </Screen>
  );
}
